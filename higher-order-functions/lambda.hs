-- A lamda function is:
--  \ args -> body

map (\x -> 2 * x + 1) [1,2,3]

-- When to use lambda expressions
-- Too simple should be written as a partial application
-- Too complex should be written as a named function
