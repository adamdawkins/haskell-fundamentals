-- Type Synonyms
-- Synonyms defines a type in terms of other types
-- the purpose of synonyms is to:
--    make code more readable
--    semantic meaning
--    completely ignored by compiler

type String = [Char]
type Point = (Double, Double)
